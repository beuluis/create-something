{
  "name": "{{ name }}",
  "version": "1.0.0",
  "description": "{{ description }}",
  "main": "src/cli.js",
  "bin": "src/cli.js",
  "files": [
    "src",
    "templates"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0",
    "test-cli": "rm -Rf test-out && node src/cli.js test-out"
  },
  "license": "{{ license }}",
  "dependencies": {
    "@beuluis/create-helper": "^1.0.4"
  }
}